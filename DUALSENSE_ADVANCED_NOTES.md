# DualSense 高级检测实现说明

## 真实输入字段与能力边界

高级页只接受 `ControllerInputSource.DualSenseHid` 的真实状态。动态演示、Xbox、缺失完整报告或蓝牙 CRC 失败时不生成通过结论。

当前真实解析并显示的字段：

- `TouchpadPressed`。
- 两个触点的控制器 Contact ID、激活位、原始 `RawX / RawY` 和归一化 `X / Y`。
- `Gyroscope X / Y / Z`：同时保留 HID signed Int16 原始计数和 `°/s`。
- `Accelerometer X / Y / Z`：同时保留 HID signed Int16 原始计数和 `g`。
- 报告 ID、报告长度、序号、时间戳、连接方式、布局名和蓝牙 CRC 状态。
- 电池 0～100% 档位，以及报告电源状态为已确认枚举时的充电中 / 已充满 / 电池供电状态。

当前未解析或未开放：

- 输入报告没有被本项目验证为“当前实际灯带 RGB”的字段，`LightbarState` 明确为 `not-parsed`。
- 灯带 USB / 蓝牙输出没有实机验证，控制项禁用。
- 自适应扳机 USB / 蓝牙输出没有实机验证，仅有独立接口、能力门控和禁用 UI 占位。
- 高级触觉反馈不是基础双通道震动的别名，本页不宣称支持。
- 没有可靠字段时电量或充电状态显示“未知”。

## 触摸板映射和测试

DualSense 原始触摸范围按 `1920 × 1080` 处理：

```text
normalizedX = clamp(rawX / 1919, 0, 1)
normalizedY = clamp(rawY / 1079, 0, 1)
pixelX = viewport.Left + normalizedX * viewport.Width
pixelY = viewport.Top  + normalizedY * viewport.Height
```

因此左上是 `(0, 0)`，右下是 `(1, 1)`；原始坐标不会直接当作 WPF 像素。原 Overlay 的四角双线性映射和所有 Overlay 资源均未修改。

`DualSenseTouchpadAnalyzer` 以 Contact ID 跟踪开始、移动、静止、结束和快速重新触摸；双指各自使用独立颜色。轨迹最多保留 1600 点，超限分批丢弃最旧点。5 秒录制和引导检测由分析器维护，不创建 `DispatcherTimer`。

覆盖率使用固定 `24 × 12` 网格：每个格子只计“经过 / 未经过”，所以同一点停留多久、设备以多高频率上报都不会提高覆盖率。边缘覆盖率只统计最外圈网格。未覆盖格只能作为复测线索，不能用一次轨迹直接判定触摸板硬件坏区。

引导流程依次验证左上到右下、右上到左下、边缘覆盖、双指同时识别和触摸板按压。

## 运动单位、坐标和融合

- 陀螺仪换算：`raw / 1024 = degree/second`。
- 加速度计换算：`raw / 8192 = g`。
- X / Y / Z 保留设备报告轴和符号，不在解析层偷偷交换或反相。
- Pitch / Roll / Yaw 是四元数融合得到的姿态角，不是把角速度直接当角度。
- 页面中的手柄姿态平面使用 Pitch、Roll、Yaw 驱动倾斜、旋转和透视近似；可切换“融合后姿态”和“纯陀螺积分姿态”。

`MotionFusionService` 使用轻量 Mahony 风格互补融合：陀螺仪提供短时旋转积分，加速度计归一化重力向量并以比例增益 `0.55` 校正 Pitch / Roll。无磁力计，因此 Yaw 没有绝对航向参考，随时间缓慢漂移属于预期现象。时间间隔大于 100 ms 被视为中断且不做大步积分；实际积分步长限制在 1～25 ms。

静止校准先等待 1 秒，再采样 3 秒，至少需要 60 个有效样本。结果包含三轴 Bias、标准差和样本数；明显移动会使校准失败。会话内立即应用 Bias，可选择保存到：

```text
%LocalAppData%\ControllerLab\DualSense\Devices\device-<SHA256前16位>.json
```

文件名只使用设备 ID 的不可逆哈希。损坏文件按“无保存配置”处理，不导致崩溃。

## 陀螺仪诊断

`DualSenseGyroscopeAnalyzer` 记录三轴正负方向响应、样本中断、异常大跳变、长时间完全冻结、校准零偏和静止噪声，并输出 0～100 软件评分。只有每个轴的正向和负向都超过 `12°/s` 才记为该轴完成双向响应；Yaw 漂移本身不作为严重异常。

静止校准仍使用现有震动互斥：开始校准前停止正在运行的震动，校准期间专业震动页不可播放。页面离开取消触摸录制 / 引导和仍在进行的静止校准；设备断开删除分析会话；UI 以 30 Hz 读取快照，后台输入采样保持独立。

## USB 与蓝牙

| 项目 | USB | 蓝牙 |
| --- | --- | --- |
| 完整输入 | `0x01`，64 bytes | `0x31`，78 bytes |
| 触摸偏移 | 33 | 34 |
| 运动字段 | 共享 body +15 / +21 | 共享 body +15 / +21 |
| CRC | 不适用 | 必须通过 CRC32；失败时拒绝触摸 / 运动结论 |
| 紧凑兼容报告 | 不适用 | `0x01` 仅基础输入，不含触摸坐标和运动字段 |
| 灯带 / 自适应扳机输出 | 待实机验证，未开放 | 待实机验证，未开放 |

上述输入布局有构造自检，不等同于真实手柄验证。本开发环境未连接 DualSense；触摸方向、物理轴向、传感器噪声、灯带和自适应扳机仍必须分别用 USB 与蓝牙实机确认。
