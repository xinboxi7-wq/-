# ControllerLab UI 与产品化优化说明

## 本次范围

本次只调整原生 WPF 信息架构、视觉令牌、页面入口、设置、历史报告和产品级状态反馈。现有 XInput、RawInput、Sony HID、设备识别、Overlay 坐标与资源、检测分析、震动输出、触摸板和传感器输入链路继续复用。

静止采样时长和死区安全余量增加了设置入口；默认值仍分别是 5 秒和原有 0.5% 安全下限。摇杆分析阈值、圆周覆盖、回中、评分、震动安全上限和 HID 报告结构没有移入 UI，也没有重复应用死区。

## 主导航与页面职责

主导航统一为七个用户任务：

1. 实时监视：设备状态、中央手柄、局部按键反馈、左右摇杆、底部 LT/RT 曲线与基础健康摘要。
2. 完整检测：保留原向导和报告页。
3. 摇杆检测：保留静止漂移、72 区间圆周、四方向回中与轨迹图；详细数字默认折叠。
4. 震动测试：保留预设、时间线、校准和紧急停止，并新增安全操作引导。
5. DualSense 高级：只在当前设备能力属于 DualSense 时显示；Xbox 下隐藏，直接路由时给出不支持说明。
6. 历史报告：查看、删除、重新检测、导出 JSON/Markdown，并支持选择两份报告比较。
7. 设置：常规、显示、检测、震动和本地数据。

设备选择与独立按键检测没有删除：设备选择保留在实时监视页顶部和断开通知动作中；按键检测保留为实时诊断卡片中的快捷入口。这样不破坏单项测试，同时避免把开发入口放在一级导航。

## 设计系统

统一令牌位于 `ControllerLabTheme.cs`：

- 间距：4 / 8 / 12 / 16 / 24 / 32 DIP。
- 圆角：卡片 16 DIP、控件 10 DIP、紧凑状态 8 DIP。
- 字号：11 / 13 / 15 / 18 / 28 / 36 DIP。
- 动效：150 / 200 / 280 ms；默认发光透明度 0.32；减少动态效果时立即完成过渡。
- 按钮：Primary、Secondary、Ghost、Danger、Icon。
- 卡片：Default、Test、Result、Device。
- 状态：Neutral、Info、Success、Warning、Error，始终同时显示文字，不只依赖颜色。
- 焦点：按钮获得键盘焦点时使用 2 DIP 蓝色边框；新增操作均提供自动化名称和键盘帮助文本。

颜色继续沿用项目的深色科技视觉：Window、Surface、Surface2、SurfaceRaised、Border、Text、Muted、Blue、Green、Warning、Red。中央手柄、底部扳机曲线、按钮局部发光和已校准 Overlay 均保留。

## 历史报告比较

选择两份报告后比较以下稳定字段：

- 综合评分。
- 左/右摇杆中心偏移。
- 左/右摇杆推荐死区。
- 左/右摇杆外圈覆盖率。

字段缺失、跳过或旧报告没有该数据时显示“未检测”，不会当作 0 或正常。变化列按“后一份减前一份”显示，报告本身的评分与原始数据不被改写。

## 设置与本地数据

设置文件：`%LocalAppData%\ControllerLab\settings.ini`。首次保存时会读取并迁移旧的 `%LocalAppData%\XboxControllerLab\settings.ini`，不会丢失已有中心偏移、参考死区和设备路由。

健康报告：`%LocalAppData%\ControllerLab\health-reports\`。

震动配置：`%LocalAppData%\ControllerLab\rumble\`。

日志：`%LocalAppData%\ControllerLab\logs\controllerlab.log`。单文件上限 512 KiB，保留 3 份轮转文件；UI 只显示自然语言错误，异常类型与调用栈写入日志。

所有范围在保存前统一校验：坐标小数位 1–3、轨迹 100–1600 点、UI 20–60 Hz、静止采样 3–10 秒、死区安全余量 0.5%–5%、默认震动不超过 40%、默认持续时间不超过 5 秒、安全强度上限 30%–70%。播放器原有 30 秒连续输出、页面离开/断开/异常归零保护继续生效。

## 连接、断开与生命周期

- 顶部连接状态区分“正在识别”“能力检测中”“已就绪”“未连接”“已断开”。
- 断开时中央手柄降低透明度，保留最近视觉与报告，不把旧数据瞬间清空。
- 断开、设备切换、离开震动页或退出应用会停止震动；运行中的摇杆、完整检测与 DualSense 临时任务会取消。
- 仍使用 `CompositionTarget.Rendering`，按设置限制为 20–60 Hz；没有重新引入多个 `DispatcherTimer`。
- 设置和报告页使用局部 `ScrollViewer`，较矮窗口不会把操作区裁掉。

## 前后变化

优化前：首页级入口同时混有设备首页、实时可视化、按键检测和硬件专项页；实时页原始坐标与调试菜单默认可见；报告历史分散在完整检测内部；没有统一产品设置、比较视图、可关闭错误通知和日志轮转。

优化后：一级导航按任务收敛；设备专属入口按能力显示；实时页保留视觉主体并默认隐藏高级数字；检测页提供操作与判定依据；历史报告成为独立页面并可比较；设置集中管理显示、检测、震动和本地数据；错误提示、焦点态、禁用态、状态色和动效由统一设计系统提供。

## 自动验证与渲染审计

新增命令：

```powershell
.\ControllerLab_UIProduct_Test.exe --product-experience-selftest
.\ControllerLab_UIProduct_Test.exe --product-ui-render-audit
```

第一条验证设置边界、历史比较、512 KiB 日志轮转和逻辑 DPI 尺寸矩阵。第二条生成：

- `audit/product-ui-2026-07-31/live-1920x1080-at-150.png`
- `audit/product-ui-2026-07-31/settings-1920x1080-at-125.png`
- `audit/product-ui-2026-07-31/history-2560x1600-at-125.png`

逻辑尺寸矩阵包含 1920×1080 的 100% / 125% / 150%，2560×1440 的 125%，2560×1600 的 150%，以及较小 1536×864 窗口。窗口最小值调整为 1020×680 DIP，使 1920×1080 在 150% 缩放下仍能放入工作区。

## 尚未实机验证

当前开发环境没有检测到 VID_045E 或 VID_054C 的在线 PnP 设备。因此本次只能确认编译、构造/运行自检、模拟数据、自检中的 USB/蓝牙报文边界、页面生命周期和离线渲染；以下仍需实机回归：

- Xbox 与 DualSense 实际按键高亮、Overlay 肉眼贴合和高 DPI 实窗表现。
- Xbox 左右震动通道体感。
- DualSense USB/蓝牙输出、触摸方向、物理陀螺仪轴向与噪声。
- 实际热插拔、设备切换和窗口跨显示器位置恢复。

未实机验证的高级触觉、灯带和自适应扳机仍保持“待验证/未开放”，没有伪造通过状态。
