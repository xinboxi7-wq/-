# ControllerLab 一键健康检测说明

## 检测与能力边界

“完整检测”直接消费现有 `ControllerState`、`JoystickAnalyzer`、`ControllerInputTestEngine` 与 `ControllerRumbleController`，不读取或改写 XInput / HID / RawInput 底层链路。Xbox 不生成 DualSense 专属步骤；DualSense 只有在当前真实报告明确提供触摸坐标或有效 Motion 数据时才生成相应步骤，否则开始页显示“当前版本暂不支持检测”。

退出当前步骤、页面离开、设备断开、设备切换和应用退出都会取消当前采样并停止震动。原设备断开后，已完成项目仅保留在内存中；原设备重连后需重新开始当前步骤。若切换成另一台设备，会话立即终止，旧结果不会写入新设备报告。

## 评分模型

分类权重为：按键 15%、摇杆 35%、扳机 20%、震动 10%、触摸板 10%、陀螺仪 10%。只有“设备支持且已经检测”的分类进入加权平均；不支持和跳过项目均不计分。任何跳过、等待或检测中的项目都会令总体结论为 `Incomplete / 检测未完成`，不会自动判定正常。

完整报告的总体阈值为：90–100 `Excellent / 优秀`，75–89 `Good / 良好`，55–74 `Attention / 需要注意`，0–54 `Poor / 状态较差`。明显摇杆漂移、严重缺失行程、无法稳定回中、扳机峰值低于 80% 或无法回零会触发严重硬件标记，并把总分上限限制为 69 分。

摇杆分类复用专业检测结果；扳机评分综合有效行程 45%、平滑度 25%、回零 20% 和满量程 10%。震动同时记录系统是否成功发送和用户对左、右、双侧通道的主观反馈。触摸板与陀螺仪不会在缺少真实解析数据时伪造通过。

## 报告存储

默认目录为 `%LocalAppData%\ControllerLab\health-reports\`。每个报告以相同 ReportId 保存：

- `health-report-<ReportId>.json`：结构化原始项目结果、指标、分类分数、总分和震动反馈。
- `health-report-<ReportId>.md`：适合阅读、截图和分享的 Markdown 摘要。

历史页面读取 JSON，支持删除同一 ReportId 的 JSON / Markdown 副本，并可将当前或历史报告导出为 JSON、Markdown 或纯文本扩展名。没有引入 PDF 依赖。

## 尚待真实手柄验证

- Xbox / 第三方 XInput 手柄的实际按键映射差异、扳机满量程和左右电机体感。
- DualSense USB 与蓝牙模式下触摸板双指覆盖、陀螺仪单位 / 阈值及基础震动通道方向。
- 专业摇杆静止、圆周和四方向回中在不同采样率、磨损程度与第三方固件下的阈值表现。
- 检测流程的预计时间与用户操作引导仍需结合真实手柄进行可用性校准。
