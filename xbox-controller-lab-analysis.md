# Xbox 手柄实验室：产品、体验与技术审查

审查日期：2026-07-16  
审查对象：`ControllerLab.exe` / `ControllerLab.cs`  
审查范围：连接状态、实时输入反馈、死区控制、中心校准、可访问性、性能与代码结构。

## 总体判断

当前版本已经是一个完成度较高的“实时可视化原型”，尤其擅长把摇杆、扳机、按键和十字键输入映射到实体手柄图上。但它离可信的“手柄实验室/诊断工具”还有一层：若干指标名称和真实计算含义不一致，125 Hz 徽标并非实时测量值，死区与校准缺少持久化和结果解释，高频 UI 重绘也还有明显优化空间。

成熟度判断：视觉演示 8.5/10；实时反馈 8/10；测量可信度 6/10；性能效率 6/10；可访问性 5.5/10；可维护性 5.5/10。

## 审查流程与健康度

1. **启动并识别手柄 — 健康**  
   能自动扫描 XInput 0–3 号设备，当前实测显示“已连接 · 玩家 1”、`xinput1_4` 和有线状态。连接入口清楚，不需要额外设置。

2. **查看实时输入 — 健康，但指标语义需修正**  
   实体手柄、摇杆轨迹、扳机百分比、按键光效和十字键方向反馈同步清楚。主要问题是右侧标为“漂移”的 X/Y 实际显示当前摇杆位置，并不是真正的静态漂移统计。

3. **调整死区 — 可操作，但作用容易被误解**  
   左右死区滑块支持鼠标、方向键、Home/End 和 UI Automation RangeValue。当前数值只改变图表中的参考圆，不会改变原始输入、过滤后的显示值或系统/游戏中的控制器行为。

4. **执行中心校准 — 基本健康，但缺少验证与持久化**  
   两秒采样、进行中提示和完成反馈都清楚。校准仅计算四个摇杆轴的均值偏移；没有稳定性检查、异常提醒、结果摘要或配置保存，重启后会恢复默认。

## 当前优势

- 原生 WPF 单文件 EXE，无 Python、Node.js 或额外运行时安装流程，离线可运行。
- XInput 动态加载带 `xinput1_4 → xinput1_3 → xinput9_1_0` 回退，兼容性思路合理。
- 物理手柄视图与诊断图表同时存在，既直观又能读出数值。
- 左绿右蓝的语义贯穿摇杆、扳机、曲线和标注，视觉语言一致。
- 十字键采用按原始手柄图片测量的四段几何轮廓，单向和斜向状态清楚。
- 死区滑块有键盘控制、可读名称、帮助文本和 RangeValue 自动化支持，这是当前可访问性最成熟的部分。
- 内置 `--demo` 模式是很有价值的视觉回归与演示工具。

## 高优先级问题

### P1 — 指标名称与真实含义不一致

- “漂移 X/Y”每 8 ms直接写入当前归一化摇杆位置；用户正常推动摇杆时它也会变化。应改名为“实时位置”，或者真正计算中心附近的均值、标准差、峰值半径和持续时间后再称为漂移。
- “死区”目前只改变图上的虚线参考圆。若产品不打算充当映射器，应明确写成“死区参考线”；若用于诊断，则应同时显示原始值和应用死区后的过滤值。
- “125 Hz”是固定文案。程序虽然计算了 `measuredHz`，但没有把结果显示出来。应分别显示“轮询频率”和“有效数据包频率”，避免把 UI 定时器间隔当成设备真实采样率。

### P1 — 自定义窗口按钮不可访问

最小化、最大化和关闭按钮在 UI Automation 中的名称是私有字体字符，且 `Focusable=false`，不能通过 Tab 键访问。应补充“最小化/最大化/关闭”的自动化名称、工具提示和可见焦点样式，并保留键盘操作。

### P1 — 校准结果不能形成可信诊断

当前校准只有“保持居中两秒”一个阶段。没有检测用户是否在采样期间移动、没有显示噪声范围、没有给出推荐死区，也不保存结果。更完整的流程应包含：中心稳定性检查 → 偏移与噪声分析 → 推荐死区 → 用户确认 → 保存为设备配置。

## 中优先级问题

### P2 — 高频绘制消耗可以明显降低

- 8 ms `DispatcherTimer` 在 UI 线程完成 XInput 调用、文本更新、插值和多块自绘区域重绘。Microsoft 文档明确说明 DispatcherTimer 受 Dispatcher 队列影响，不保证精确在目标间隔执行，因此它不适合作为“真实 125 Hz”测量依据。
- 每个输入周期都调用电池查询；电池状态无需 125 次/秒，1–5 秒一次已经足够。
- 每帧重复创建 Brush、Pen、FormattedText、渐变和几何变换，且死区值即使没变化也会反复使图表失效。
- 当前实测每个空闲实例约占一个逻辑核心的 21–22%，工作集约 107–116 MB。机器上同时存在两个实例，而代码没有单实例保护，会把资源消耗直接翻倍。

建议把“输入采集”和“界面渲染”拆开：后台读取最新 XInput 状态，UI 使用显示器刷新节奏更新；只有输入包、动画或状态真正变化时才重绘；缓存可冻结的画笔、文字和几何对象；电池与设备元数据低频刷新。

### P2 — 电池类型映射有逻辑错误

代码把 `BatteryType == 0xFF` 显示为“未连接”。Microsoft 的 XInput 定义中，“未知电池类型”和“设备未连接”是两个不同状态；这会把未知电池类型误报为断开。应分别处理 disconnected、wired、alkaline、NiMH 和 unknown。

### P2 — 信息很多，但诊断结论不足

界面同时显示实体手柄、两组摇杆图和两张扳机曲线，信息密度高；但用户仍需要自己判断“有没有问题”。建议增加一个紧凑的诊断摘要：中心偏移、最大漂移、推荐死区、扳机是否到底、按键是否卡住、数据包抖动，并用“正常/注意/异常”给出结论。

### P2 — 扳机曲线目前大部分是静态装饰

图中的对角线始终是输入=输出，没有可调响应曲线。对于监视器，更有价值的是最近 2–5 秒的时间序列、最小/最大值、回弹时间和是否能达到 100%；若保留响应曲线，应允许设置曲线并对比原始/处理后输出。

## 可访问性风险

- 已确认优点：主要按钮有中文名称；死区滑块可键盘操作并暴露 0–0.25 的范围值。
- 已确认问题：自定义窗口按钮缺少可读名称且不进入 Tab 顺序。
- 可能问题：连接、断开、校准完成等动态状态没有 LiveRegion 通知，屏幕阅读器可能不会主动播报。
- 可能问题：持续移动、发光和轨迹没有“减少动态效果/暂停动画”选项。
- 截图显示绿色与蓝色之外还有左/右文字，因此不是完全依赖颜色；但本次未做逐像素 WCAG 对比度测量，不能宣称完整合规。

## 技术结构评价

当前 1,468 行逻辑集中在一个 `ControllerLab.cs` 中，包含窗口构建、输入层、模型、绘制、滑块、自动化接口和图表。短期修改快，但继续增加设备选择、记录、振动测试和配置保存后会迅速变难维护。

建议拆为：

- `Input/XInputProvider.cs`：XInput 动态加载、状态读取、电池与设备能力。
- `Models/ControllerState.cs`：原始值、归一化值、过滤值和统计指标。
- `Services/SamplingService.cs`：采集线程、包频率、抖动和历史缓冲。
- `Services/ProfileStore.cs`：按设备保存校准与死区配置。
- `Views/ControllerVisual.cs`、`StickPlot.cs`、`TriggerHistory.cs`：独立绘制组件。
- `MainWindow`/ViewModel：仅负责界面状态与命令。
- 单元测试：归一化边界、角度、按键掩码、电池枚举、死区过滤、校准稳定性。

## 建议迭代顺序

1. **可信度修复**：修正“漂移/死区/125 Hz/电池状态”的语义和显示。
2. **性能修复**：电池低频轮询、按变化重绘、缓存绘制对象、限制单实例。
3. **实验室能力**：校准向导、结果评分、配置持久化、数据包频率与抖动。
4. **可访问性**：窗口按钮名称与焦点、LiveRegion、减少动画。
5. **扩展功能**：控制器选择、会话记录/CSV、振动测试、紧凑悬浮模式；需要更广设备信息时再评估 GameInput。Microsoft 将 GameInput描述为 XInput、DirectInput、Raw Input、HID 和 WinRT 输入能力的功能超集，但对当前“只做 Xbox/XInput”范围而言并非第一优先级。

## 当前审查证据

- `audit-2026-07-16/01-connected-dashboard.png`：真实手柄连接、静止状态。
- `audit-2026-07-16/02-dynamic-input.png`：摇杆、扳机、轨迹与十字键动态状态。
- `audit-2026-07-16/03-calibration-running.png`：两秒校准进行中。
- `audit-2026-07-16/04-calibration-complete.png`：校准完成与偏移结果。
- UI Automation：56 个后代元素；两组死区滑块均可聚焦，窗口控制按钮不可聚焦且名称为字体字符。
- 性能抽样：两个运行实例各抽样 3 秒，约占一个逻辑核心 21–22%，工作集 107–116 MB；结果只代表当前机器与当前状态。

## 证据限制

- 没有高速摄像机或硬件回环，无法测量从实体按压到屏幕反馈的端到端延迟。
- 当前连接为有线手柄，没有验证蓝牙电池状态和断线重连全过程。
- UI Automation 可以检查名称、焦点和控件模式，但不能替代 NVDA/Narrator 的实际朗读测试。
- 性能只做了短时间现场抽样，不等同于完整 ETW、GPU 或 GC 分析。

## 官方技术参考

- Microsoft: XInputGetState 支持 0–3 用户索引并返回当前控制器状态：<https://learn.microsoft.com/en-us/windows/win32/api/xinput/nf-xinput-xinputgetstate>
- Microsoft: XINPUT_BATTERY_INFORMATION 区分断开、有线、已知电池类型和未知类型：<https://learn.microsoft.com/en-us/windows/win32/api/xinput/ns-xinput-xinput_battery_information>
- Microsoft: WPF DispatcherTimer 不保证在目标间隔精确执行：<https://learn.microsoft.com/en-us/dotnet/api/system.windows.threading.dispatchertimer?view=netframework-4.7.2>
- Microsoft: GameInput 是传统输入 API 的功能超集：<https://learn.microsoft.com/en-us/windows/win32/xinput/xinput-game-controller-apis-portal>
