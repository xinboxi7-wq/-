# ControllerLab 震动测试版

## 使用

1. 连接真实 Xbox/XInput 或 DualSense 手柄。
2. 打开顶部“震动测试”。
3. 先用默认 `40% / 5 秒`，分别测试左侧和右侧通道。
4. 可继续测试均衡、渐强、脉冲和左右交替。
5. 任意时候都可点击“停止震动”。

## 安全保护

- 单次持续时间最大 30 秒。
- 离开震动页面、切换设备、设备断开、输出异常或退出应用时自动发送停止命令。
- 同一设备同一时间只有一个震动任务；重复开始会先取消并清零上一个任务。
- 漂移检测运行期间禁止震动；震动停止后需等待至少 1 秒才能开始漂移采样。

## 设备说明

- Xbox/XInput：左通道映射低频大马达，右通道映射高频小马达，范围为 `0..65535`。
- DualSense USB：使用输出报告 `0x02`，长度 63 字节。
- DualSense 蓝牙：使用输出报告 `0x31`，长度 78 字节，并计算输出 CRC32。
- DualSense 两种连接方式的报告构造已通过逻辑自检，但仍需真实手柄确认震感和 Windows HID 写入兼容性；程序不会把构造成功伪装成实机输出成功。

## 建议反馈

请记录手柄型号、连接方式、左/右通道是否正确、每种预设是否正常停止，以及页面显示的完整错误信息。
