# ControllerLab 专业震动测试版

## 使用

1. 连接真实 Xbox/XInput 或 DualSense 手柄。
2. 打开顶部“震动测试”。
3. 先用默认 `40% / 5 秒`，分别测试左侧和右侧通道。
4. 可使用 14 个数据化预设，或在时间线中新增 / 删除节点并调整时间、左右强度和线性插值。
5. 自定义预设可保存、重新读取和删除；配置目录见下文。
6. 感知校准依次记录左右最小可感知强度和舒适上限，自动渐强最高只到 65%。
7. 任意时候都可点击醒目的“紧急停止”。

## 安全保护

- 单次持续时间最大 30 秒。
- 离开震动页面、切换设备、设备断开、输出异常或退出应用时自动发送停止命令。
- 同一设备同一时间只有一个震动任务；重复开始会先取消并清零上一个任务。
- 专业时间线由独立 `RumblePatternPlayer` 以 25 Hz 输出，不创建 DispatcherTimer。
- 0% 不启动输出；高于 80% 最长 3 秒，高于 60% 最长 8 秒，高于 40% 最长 15 秒。
- 漂移检测、陀螺仪静止校准与完整检测静止采样期间禁止震动；震动停止后需等待至少 1 秒才能开始漂移采样。

## 设备说明

- Xbox/XInput：左通道映射低频大马达，右通道映射高频小马达，范围为 `0..65535`。
- DualSense USB：使用输出报告 `0x02`，长度 63 字节。
- DualSense 蓝牙：使用输出报告 `0x31`，长度 78 字节，并计算输出 CRC32。
- DualSense 两种连接方式的报告构造已通过逻辑自检，但仍需真实手柄确认震感和 Windows HID 写入兼容性；程序不会把构造成功伪装成实机输出成功。
- DualSense 高级触觉反馈与自适应扳机严格保持关闭；基础兼容震动不会被描述为完整触觉反馈。

## 配置位置

- 根目录：`%LocalAppData%\ControllerLab\rumble\`
- 设备配置：`devices\device-<SHA256 截断哈希>.json`
- 自定义时间线：`custom-patterns.json`

设备文件名不保存原始 DeviceId。默认左右强度最多 40%，默认持续时间最多 5 秒。

## 建议反馈

请记录手柄型号、连接方式、左/右通道是否正确、每种预设是否正常停止，以及页面显示的完整错误信息。
